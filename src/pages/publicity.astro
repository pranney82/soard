---
/**
 * Publicity Page — Sunshine on a Ranney Day
 *
 * Editorial magazine-spread. Restraint over decoration.
 *
 * Design ceiling patterns used:
 * - Edge-to-edge split (homepage featured story)
 * - Horizontal scroll carousel with scroll-snap (homepage kids strip)
 * - Sticky photo column (homepage process section)
 * - Overlapping photo composition (homepage mission)
 * - Staggered cascade reveals with cubic-bezier spring
 * - YouTube click-to-load facade with preconnect warming
 * - Native <details> for collapsible year groups (donate FAQ)
 * - JSON-LD CollectionPage + NewsArticle structured data
 */
import PageLayout from '../layouts/PageLayout.astro';
import MediaLogos from '../components/ui/MediaLogos.astro';
import CFImage from '../components/ui/CFImage.astro';
import { CF_BASE as CF } from '../utils/cf-image';
import { getCollection } from 'astro:content';

const allPress = await getCollection('press');
const press = allPress.sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime());
const featured = press.filter(p => p.data.featured);
const heroFeatured = featured[0];
const restFeatured = featured.slice(1);

const years = [...new Set(press.map(p => new Date(p.data.date).getFullYear()))].sort((a, b) => b - a);
const pressByYear: Record<number, typeof press> = {};
years.forEach(y => { pressByYear[y] = press.filter(p => new Date(p.data.date).getFullYear() === y); });

const categories = ['all', 'national-tv', 'local-tv', 'print', 'magazine', 'entertainment'] as const;
const categoryLabels: Record<string, string> = {
  'all': 'All', 'national-tv': 'National TV', 'local-tv': 'Local TV',
  'print': 'Print & Online', 'magazine': 'Magazine', 'entertainment': 'Entertainment',
};

function formatDate(dateStr: string): string {
  const d = new Date(dateStr + 'T00:00:00');
  return d.toLocaleDateString('en-US', { month: 'long', year: 'numeric' });
}
function formatShortDate(dateStr: string): string {
  const d = new Date(dateStr + 'T00:00:00');
  return d.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' });
}

const celebrities = [
  { name: 'Atlanta Braves', desc: 'Holly & Peter honored as Community Heroes with a $5,000 donation from players Dansby Swanson, Max Fried, Charlie Culberson & Ender Inciarte.', imageId: 'press-braves-community-heroes', type: 'Community Heroes' },
  { name: 'Coach Nick Saban', desc: 'Coach Saban surprised cancer survivor Ally with a meeting with her favorite coach.', imageId: 'press-nick-saban-ally', type: 'Celebrity Surprise' },
  { name: 'Atlanta Hawks', desc: 'Dominique Wilkins, Tim Hardaway Jr. & more surprised Kalen with his Hawks-themed dream bedroom.', imageId: 'press-hawks-kalen-wilkins', type: 'Celebrity Surprise' },
  { name: 'Coach Kirby Smart', desc: 'Supported by the UGA community and their football coach and family.', imageId: 'press-kirby-smart-uga', type: 'Community Partner' },
  { name: 'Atlanta Falcons', desc: 'Peter & Holly honored as Everyday Heroes by the Falcons.', imageId: 'press-falcons-everyday-heroes', type: 'Everyday Heroes' },
  { name: "TLC's Say Yes to the Dress", desc: "Lori & Monte helped with makeovers fit for princesses.", imageId: 'press-tlc-lori-monte', type: 'Celebrity Partner' },
  { name: 'Coy Bowles — Zac Brown Band', desc: "Grammy-winning guitarist surprised Ruby in her new ballet studio.", imageId: 'press-coy-bowles-ruby', type: 'Celebrity Surprise' },
  { name: 'Governor Nathan Deal', desc: "Governor & Mrs. Deal attended the Evening of Sunshine Gala.", imageId: 'press-governor-deal-gala', type: 'Evening of Sunshine' },
];

const totalPress = press.length;
const tvCount = press.filter(p => p.data.category === 'national-tv' || p.data.category === 'local-tv').length;
const uniqueOutlets = new Set(press.map(p => p.data.outlet)).size;

const jsonLd = {
  '@context': 'https://schema.org', '@type': 'CollectionPage',
  name: 'Publicity & Media Coverage',
  description: `Featured on the Today Show, WSB-TV, 11Alive, Fox 5, AJC, K-LOVE, and ${totalPress}+ media outlets.`,
  url: 'https://sunshineonaranneyday.com/publicity',
  mainEntity: { '@type': 'ItemList', numberOfItems: totalPress,
    itemListElement: featured.slice(0, 8).map((item, i) => ({
      '@type': 'ListItem', position: i + 1,
      item: { '@type': 'NewsArticle', headline: item.data.title,
        publisher: { '@type': 'Organization', name: item.data.outlet },
        datePublished: item.data.date, url: item.data.url },
    })),
  },
};
const breadcrumbs = [{ name: 'Home', url: '/' }, { name: 'Publicity', url: '/publicity' }];
---

<PageLayout
  title="Publicity & Media Coverage"
  description={`Featured on the Today Show, WSB-TV, 11Alive, Fox 5, AJC, K-LOVE, and ${totalPress}+ media outlets.`}
  heroLabel="In the Spotlight"
  heroTitle={'Our story, told by <em class="highlight">those who know it best</em>.'}
  heroSubtitle="From the Today Show to Coach Saban, from the Atlanta Braves to K-LOVE — the world is helping us share the mission of building dreams for children with special needs."
  ogImage={`${CF}/press-braves-community-heroes/w=1200,h=630,fit=cover,q=80`}
  jsonLd={jsonLd}
  breadcrumbs={breadcrumbs}
>

  <!-- ════ STATS ════ -->
  <section class="stats" aria-label="Media impact" data-reveal>
    <div class="container">
      <div class="stats__row">
        <div class="stats__item">
          <span class="stats__num">{totalPress}+</span>
          <span class="stats__label">Media Features</span>
        </div>
        <span class="stats__div" aria-hidden="true"></span>
        <div class="stats__item">
          <span class="stats__num">{uniqueOutlets}+</span>
          <span class="stats__label">Outlets</span>
        </div>
        <span class="stats__div" aria-hidden="true"></span>
        <div class="stats__item">
          <span class="stats__num">{tvCount}</span>
          <span class="stats__label">TV Segments</span>
        </div>
        <span class="stats__div" aria-hidden="true"></span>
        <div class="stats__item">
          <span class="stats__num">2</span>
          <span class="stats__label">National TV</span>
        </div>
      </div>
    </div>
  </section>

  <MediaLogos theme="dark" linked={false} />

  <!-- ════ TODAY SHOW — EDGE-TO-EDGE SPLIT (homepage featured story pattern) ════ -->
  <section class="today" aria-labelledby="today-title">
    <div class="today__split">
      <figure class="today__video">
        <div class="today__frame" id="yt-today-show">
          <img
            src="https://i.ytimg.com/vi/Tej9V0MPQbY/maxresdefault.jpg"
            alt="Today Show — Sunshine on a Ranney Day"
            width="1280" height="720" loading="lazy" decoding="async"
            class="today__poster"
          />
          <button class="today__play" aria-label="Play Today Show video" data-yt-id="Tej9V0MPQbY">
            <span class="today__play-circle">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg>
            </span>
          </button>
        </div>
        <span class="today__tag">Featured</span>
      </figure>
      <div class="today__content" data-reveal>
        <div class="section-label">NBC Today Show</div>
        <h2 id="today-title">Renovating hospital sleep rooms for families of sick children.</h2>
        <p>Peter & Holly Ranney were featured on NBC's Today Show as part of the #ShareKindness campaign for their renovation of parent sleep rooms at Children's Healthcare of Atlanta — transforming hospital spaces into calming retreats.</p>
        <a href="https://www.today.com/kindness/hospital-sleep-room-makeover-helps-parents-sick-children-t106165" class="btn btn-dark" target="_blank" rel="noopener noreferrer">Read the Story →</a>
      </div>
    </div>
  </section>

  <!-- ════ BRAVES — SECONDARY VIDEO ════ -->
  <section class="vid-secondary" aria-labelledby="braves-title">
    <div class="container">
      <div class="vid-secondary__split" data-reveal>
        <div class="vid-secondary__frame">
          <CFImage
            src={`${CF}/press-braves-community-heroes/public`}
            alt="Atlanta Braves Community Heroes"
            width={800} height={450}
            sizes="(max-width: 768px) 100vw, 480px"
            widths={[400, 560, 800]}
            class="vid-secondary__poster"
          />
          <a href="https://www.mlb.com/braves/community/serve/community-heroes-week/2018-honorees" target="_blank" rel="noopener noreferrer" class="vid-secondary__play" aria-label="Watch on MLB.com">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg>
          </a>
        </div>
        <div class="vid-secondary__text">
          <div class="section-label">MLB.com</div>
          <h3 id="braves-title">Atlanta Braves Community Heroes</h3>
          <p>Braves players surprise the Ranneys at a makeover and honor them on-field during Community Heroes Week.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ════ CELEBRITY SUPPORTERS — HORIZONTAL SCROLL (homepage kids strip pattern) ════ -->
  <section class="celebs" aria-labelledby="celebs-heading">
    <div class="celebs__header" data-reveal>
      <div>
        <div class="section-label">Notable Supporters</div>
        <h2 id="celebs-heading">Championed by <em class="highlight">Atlanta's best</em></h2>
      </div>
      <p class="celebs__sub">From coaches and athletes to TV personalities and Georgia's governor.</p>
    </div>
    <div class="celebs__scroll-wrap">
      <ul class="celebs__scroll">
        {celebrities.map(c => (
          <li class="celebs__item">
            <div class="celebs__card">
              <div class="celebs__photo">
                <CFImage
                  src={`${CF}/${c.imageId}/public`}
                  alt={c.name}
                  width={440} height={520}
                  sizes="320px"
                  widths={[320, 440, 640]}
                  fit="cover"
                  gravity="face"
                  class="celebs__img"
                />
                <div class="celebs__gradient"></div>
                <span class="celebs__type">{c.type}</span>
              </div>
              <div class="celebs__info">
                <h3 class="celebs__name">{c.name}</h3>
                <p class="celebs__desc">{c.desc}</p>
              </div>
            </div>
          </li>
        ))}
      </ul>
      <button class="celebs__btn celebs__btn--prev" aria-label="Scroll left" type="button">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M15 18l-6-6 6-6"/></svg>
      </button>
      <button class="celebs__btn celebs__btn--next" aria-label="Scroll right" type="button">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M9 18l6-6-6-6"/></svg>
      </button>
    </div>
  </section>

  <!-- ════ PULL QUOTE ════ -->
  <section class="pq" aria-label="Press quote" data-reveal>
    <div class="container container--narrow">
      <blockquote class="pq__block">
        <span class="pq__mark" aria-hidden="true">&ldquo;</span>
        <p class="pq__text">K-LOVE, America's most-listened-to Christian radio network, profiled Holly Ranney in their Positive People series — covering SOARD's founding story and 250+ families helped.</p>
        <footer class="pq__cite"><cite>— K-LOVE Positive People</cite></footer>
      </blockquote>
    </div>
  </section>

  <!-- ════ FEATURED COVERAGE ════ -->
  <section class="feat" aria-labelledby="feat-heading">
    <div class="container">
      <div data-reveal>
        <div class="section-label">Featured Stories</div>
        <h2 id="feat-heading" class="sec-title">Highlighted <em class="highlight">coverage</em></h2>
      </div>

      {heroFeatured && (
        <article class="feat__hero" data-reveal>
          <div class="feat__hero-accent" aria-hidden="true"></div>
          <div class="feat__hero-inner">
            <div class="feat__hero-content">
              <span class="feat__cat">{categoryLabels[heroFeatured.data.category]}</span>
              <h3 class="feat__hero-title">{heroFeatured.data.title}</h3>
              <p class="feat__hero-outlet">{heroFeatured.data.outlet} · {formatShortDate(heroFeatured.data.date)}</p>
              <p class="feat__hero-excerpt">{heroFeatured.data.excerpt}</p>
              {heroFeatured.data.url && (
                <a href={heroFeatured.data.url} class="feat__hero-link" target="_blank" rel="noopener noreferrer">
                  Read the full story <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 17L17 7"/><path d="M7 7h10v10"/></svg>
                </a>
              )}
            </div>
            <div class="feat__hero-deco" aria-hidden="true">
              <span class="feat__hero-qm">&ldquo;</span>
            </div>
          </div>
        </article>
      )}

      <div class="feat__grid">
        {restFeatured.map((item, i) => (
          <article class="fc" data-reveal data-delay={i < 3 ? String(i) : undefined}>
            <div class="fc__bar" aria-hidden="true"></div>
            <div class="fc__body">
              <div class="fc__top">
                <span class="feat__cat">{categoryLabels[item.data.category]}</span>
                <time datetime={item.data.date}>{formatShortDate(item.data.date)}</time>
              </div>
              <h3 class="fc__title">{item.data.title}</h3>
              <p class="fc__outlet">{item.data.outlet}</p>
              <p class="fc__excerpt">{item.data.excerpt}</p>
              {item.data.url && (
                <a href={item.data.url} class="fc__link" target="_blank" rel="noopener noreferrer">
                  Read <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 17L17 7"/><path d="M7 7h10v10"/></svg>
                </a>
              )}
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- ════ PRESS ARCHIVE ════ -->
  <section class="archive" aria-labelledby="arch-heading" id="archive">
    <div class="container">
      <div class="archive__header" data-reveal>
        <div>
          <div class="section-label">Press Archive</div>
          <h2 id="arch-heading" class="sec-title">All <em class="highlight">{totalPress} stories</em></h2>
        </div>
        <nav class="archive__tabs" aria-label="Filter by category">
          {categories.map(cat => (
            <button class={`ftab ${cat === 'all' ? 'ftab--active' : ''}`} data-filter={cat} aria-pressed={cat === 'all' ? 'true' : 'false'}>
              {categoryLabels[cat]}
              <span class="ftab__ct">{cat === 'all' ? totalPress : press.filter(p => p.data.category === cat).length}</span>
            </button>
          ))}
        </nav>
      </div>
      <p class="archive__live" aria-live="polite" aria-atomic="true"></p>
      <div class="archive__body">
        {years.map((year, yi) => (
          <details class="yg" data-year={year} open={yi === 0} data-reveal>
            <summary class="yg__sum">
              <span class="yg__year">{year}</span>
              <span class="yg__ct">{pressByYear[year].length}</span>
              <span class="yg__line" aria-hidden="true"></span>
              <span class="yg__chev" aria-hidden="true"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M6 9l6 6 6-6"/></svg></span>
            </summary>
            <div class="cl">
              {pressByYear[year].map(item => (
                <article class="ci" data-category={item.data.category}>
                  <div class="ci__bar" aria-hidden="true"></div>
                  <div class="ci__content">
                    <div class="ci__meta">
                      <span class="ci__outlet">{item.data.outlet}</span>
                      <span class="ci__dot" aria-hidden="true"></span>
                      <time datetime={item.data.date}>{formatDate(item.data.date)}</time>
                      <span class="ci__tag">{categoryLabels[item.data.category]}</span>
                    </div>
                    <h3 class="ci__title">
                      {item.data.url ? <a href={item.data.url} target="_blank" rel="noopener noreferrer">{item.data.title}</a> : <span>{item.data.title}</span>}
                    </h3>
                    <p class="ci__excerpt">{item.data.excerpt}</p>
                  </div>
                  {item.data.url && (
                    <a href={item.data.url} class="ci__arrow" target="_blank" rel="noopener noreferrer" aria-label={`Read: ${item.data.title}`}>
                      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 17L17 7"/><path d="M7 7h10v10"/></svg>
                    </a>
                  )}
                </article>
              ))}
            </div>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- ════ CTA ════ -->
  <section class="cta" data-reveal>
    <div class="cta__split">
      <div class="cta__content">
        <div class="section-label section-label--on-dark">Media</div>
        <h2>Tell our story <em class="highlight">with us</em></h2>
        <p>We provide press-ready assets, high-resolution photography, and direct access to founders Peter & Holly Ranney.</p>
        <div class="cta__actions">
          <a href="mailto:info@soardcharity.com?subject=Media%20Inquiry" class="btn btn-primary btn-lg">Media Inquiry →</a>
          <a href="/branding" class="btn btn-outline-light">Brand & Media Kit</a>
        </div>
      </div>
    </div>
  </section>

</PageLayout>

<script>
  document.addEventListener('astro:page-load', () => {
    /* ── YouTube facade ── */
    let warmed = false;
    function warm() {
      if (warmed) return; warmed = true;
      ['https://www.youtube-nocookie.com','https://i.ytimg.com'].forEach(o => {
        const l = document.createElement('link'); l.rel='preconnect'; l.href=o; document.head.appendChild(l);
      });
    }
    document.querySelectorAll<HTMLButtonElement>('[data-yt-id]').forEach(btn => {
      btn.addEventListener('pointerover', warm, { once: true });
      btn.addEventListener('focus', warm, { once: true });
      btn.addEventListener('click', () => {
        const id = btn.dataset.ytId, wrap = btn.closest('.today__frame');
        if (!wrap || !id) return;
        const f = document.createElement('iframe');
        f.src = `https://www.youtube-nocookie.com/embed/${id}?autoplay=1`;
        f.title = btn.getAttribute('aria-label') || '';
        f.allow = 'accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture';
        f.allowFullscreen = true;
        f.style.cssText = 'position:absolute;inset:0;width:100%;height:100%;border:0;z-index:4;';
        wrap.appendChild(f);
        wrap.querySelector('.today__poster')?.remove();
        btn.remove();
      });
    });

    /* ── Celebrity carousel (homepage kids strip pattern) ── */
    const scroll = document.querySelector('.celebs__scroll') as HTMLElement|null;
    const prev = document.querySelector('.celebs__btn--prev') as HTMLElement|null;
    const next = document.querySelector('.celebs__btn--next') as HTMLElement|null;
    if (scroll && prev && next) {
      function update() {
        const atStart = scroll!.scrollLeft <= 8;
        const atEnd = scroll!.scrollLeft + scroll!.clientWidth >= scroll!.scrollWidth - 8;
        prev!.classList.toggle('is-active', !atStart);
        next!.classList.toggle('is-active', !atEnd);
      }
      prev.addEventListener('click', () => scroll.scrollBy({ left: -680, behavior: 'smooth' }));
      next.addEventListener('click', () => scroll.scrollBy({ left: 680, behavior: 'smooth' }));
      scroll.addEventListener('scroll', update, { passive: true });
      update();
    }

    /* ── Archive filters ── */
    const tabs = document.querySelectorAll<HTMLButtonElement>('.ftab');
    const items = document.querySelectorAll<HTMLElement>('.ci[data-category]');
    const groups = document.querySelectorAll<HTMLDetailsElement>('.yg[data-year]');
    const live = document.querySelector<HTMLElement>('.archive__live');
    tabs.forEach(tab => {
      tab.addEventListener('click', () => {
        const cat = tab.dataset.filter || 'all';
        tabs.forEach(t => { t.classList.remove('ftab--active'); t.setAttribute('aria-pressed','false'); });
        tab.classList.add('ftab--active'); tab.setAttribute('aria-pressed','true');
        let n = 0;
        items.forEach(ci => { const ok = cat==='all'||ci.dataset.category===cat; ci.style.display=ok?'':'none'; if(ok) n++; });
        groups.forEach(g => {
          const any = Array.from(g.querySelectorAll<HTMLElement>('.ci[data-category]')).some(ci=>ci.style.display!=='none');
          g.style.display = any ? '' : 'none';
          if (any && cat!=='all') g.open = true;
        });
        if (live) live.textContent = cat==='all' ? '' : `Showing ${n} ${n===1?'story':'stories'}`;
      });
    });
  });
</script>

<style>
  /* ══════════════════════
     FOUNDATIONS
     ══════════════════════ */
  .container { max-width: var(--max-width); margin: 0 auto; padding-inline: var(--page-padding); }
  .container--narrow { max-width: var(--max-width-narrow); }
  .section-label { font-size: 12px; font-weight: 700; letter-spacing: 0.14em; text-transform: uppercase; color: var(--c-accent); display: flex; align-items: center; gap: 12px; margin-bottom: 20px; }
  .section-label::before { content:''; width: 28px; height: 2px; background: currentColor; flex-shrink: 0; }
  .section-label--on-dark { color: var(--c-yellow); }
  .sec-title { font-family: var(--font-display); font-size: clamp(1.6rem,3.5vw,2.4rem); line-height: 1.2; margin-bottom: var(--space-lg); color: var(--c-text); }
  [data-reveal] { opacity:0; transform: translateY(28px); transition: opacity 0.7s cubic-bezier(0.16,1,0.3,1), transform 0.7s cubic-bezier(0.16,1,0.3,1); }
  [data-reveal].is-visible { opacity:1; transform: translateY(0); }
  [data-delay="1"] { transition-delay: 0.1s; }
  [data-delay="2"] { transition-delay: 0.2s; }
  [data-delay="3"] { transition-delay: 0.3s; }

  /* ══════════════════════
     STATS
     ══════════════════════ */
  .stats { background: var(--c-dark); padding: var(--space-2xl) 0; }
  .stats__row { display: flex; align-items: center; justify-content: center; flex-wrap: wrap; }
  .stats__item { text-align: center; padding: var(--space-md) var(--space-2xl); }
  .stats__num { display: block; font-family: var(--font-display); font-size: clamp(2rem,4vw,2.8rem); font-weight: 700; color: var(--c-yellow); line-height: 1; }
  .stats__label { display: block; font-size: 0.72rem; font-weight: 600; color: rgba(255,255,255,0.55); text-transform: uppercase; letter-spacing: 0.1em; margin-top: 6px; }
  .stats__div { width: 1px; height: 36px; background: rgba(255,255,255,0.1); flex-shrink: 0; }

  /* ══════════════════════
     TODAY SHOW — EDGE-TO-EDGE SPLIT
     ══════════════════════ */
  .today { background: var(--c-cream); contain: layout style; }
  .today__split { display: grid; grid-template-columns: 1.15fr 1fr; min-height: 560px; }
  .today__video { position: relative; overflow: hidden; margin: 0; }
  .today__frame { position: absolute; inset: 0; background: #000; cursor: pointer; }
  .today__poster { width: 100%; height: 100%; object-fit: cover; display: block; }
  .today__play {
    position: absolute; top: 50%; left: 50%; transform: translate(-50%,-50%);
    background: none; border: none; cursor: pointer; z-index: 2; padding: 0;
    transition: transform 0.3s cubic-bezier(0.16,1,0.3,1);
  }
  .today__play:hover { transform: translate(-50%,-50%) scale(1.08); }
  .today__play-circle {
    display: flex; align-items: center; justify-content: center;
    width: 72px; height: 72px; border-radius: 50%;
    background: rgba(255,218,36,0.95); color: var(--c-dark);
    box-shadow: 0 0 0 0 rgba(255,218,36,0.4);
    animation: pulse 2.5s ease-out infinite;
  }
  @keyframes pulse {
    0% { box-shadow: 0 0 0 0 rgba(255,218,36,0.35); }
    70% { box-shadow: 0 0 0 18px rgba(255,218,36,0); }
    100% { box-shadow: 0 0 0 0 rgba(255,218,36,0); }
  }
  .today__tag {
    position: absolute; top: 24px; left: 24px;
    background: var(--c-yellow); color: var(--c-dark);
    font-size: 12px; font-weight: 700; padding: 6px 16px;
    border-radius: 50px; letter-spacing: 0.04em; z-index: 1;
  }
  .today__content { padding: 72px 64px; display: flex; flex-direction: column; justify-content: center; }
  .today__content h2 { font-family: var(--font-display); font-size: clamp(24px,3vw,32px); line-height: 1.3; margin-bottom: 20px; color: var(--c-text); }
  .today__content p { font-size: 16px; line-height: 1.8; margin-bottom: 28px; color: var(--c-text-muted); }

  /* ══════════════════════
     SECONDARY VIDEO
     ══════════════════════ */
  .vid-secondary { padding: 100px 0; background: var(--c-dark); contain: layout style; }
  .vid-secondary__split { display: grid; grid-template-columns: minmax(0,480px) 1fr; gap: var(--space-3xl); align-items: center; }
  .vid-secondary__frame { position: relative; aspect-ratio: 16/9; border-radius: 16px; overflow: hidden; background: #000; }
  .vid-secondary__poster :global(img) { width:100%; height:100%; object-fit: cover; }
  .vid-secondary__play {
    position: absolute; top:50%; left:50%; transform: translate(-50%,-50%);
    width: 56px; height: 56px; border-radius: 50%;
    background: rgba(206,17,65,0.9); color: white;
    display: flex; align-items: center; justify-content: center;
    transition: transform 0.25s cubic-bezier(0.16,1,0.3,1), background 0.2s;
    text-decoration: none;
  }
  .vid-secondary__play:hover { transform: translate(-50%,-50%) scale(1.08); background: #CE1141; }
  .vid-secondary__text h3 { font-family: var(--font-display); font-size: clamp(20px,2.5vw,28px); color: white; margin-bottom: 12px; line-height: 1.3; }
  .vid-secondary__text p { font-size: 15px; line-height: 1.75; color: rgba(255,255,255,0.55); }
  .vid-secondary__text .section-label { color: var(--c-yellow); }

  /* ══════════════════════
     CELEBRITIES — HORIZONTAL SCROLL
     ══════════════════════ */
  .celebs { padding: 100px 0; overflow: hidden; contain: layout style; }
  .celebs__header {
    max-width: var(--max-width); margin: 0 auto 48px; padding: 0 var(--page-padding);
    display: flex; align-items: flex-end; justify-content: space-between; flex-wrap: wrap; gap: 16px;
  }
  .celebs__header h2 { margin: 0; font-family: var(--font-display); font-size: clamp(1.5rem,3vw,2.2rem); line-height: 1.2; }
  .celebs__header h2 em { font-style: italic; }
  .celebs__sub { font-size: 15px; color: var(--c-text-muted); max-width: 360px; line-height: 1.6; }
  .celebs__scroll-wrap { position: relative; }
  .celebs__scroll {
    display: flex; gap: 24px; padding: 0 var(--page-padding);
    overflow-x: auto; scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch; scrollbar-width: none;
    list-style: none; margin: 0; scroll-behavior: smooth;
  }
  .celebs__scroll::-webkit-scrollbar { display: none; }
  .celebs__item { flex-shrink: 0; width: 300px; scroll-snap-align: start; }
  .celebs__card { display: block; text-decoration: none; color: inherit; }
  .celebs__photo {
    width: 300px; height: 360px; border-radius: 20px;
    overflow: hidden; margin-bottom: 16px; position: relative; background: var(--c-warm-gray);
  }
  .celebs__photo :global(img) { width:100%; height:100%; object-fit: cover; transition: transform 0.5s cubic-bezier(0.16,1,0.3,1); }
  .celebs__card:hover .celebs__photo :global(img) { transform: scale(1.04); }
  .celebs__gradient { position: absolute; inset: 0; background: linear-gradient(0deg, rgba(0,0,0,0.6) 0%, rgba(0,0,0,0) 50%); pointer-events: none; }
  .celebs__type {
    position: absolute; bottom: 16px; left: 16px;
    font-size: 0.55rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.12em;
    color: var(--c-yellow); background: rgba(0,0,0,0.4); backdrop-filter: blur(8px);
    padding: 4px 10px; border-radius: 4px;
  }
  .celebs__name { font-family: var(--font-display); font-size: 17px; margin-bottom: 4px; }
  .celebs__desc { font-size: 13px; color: var(--c-text-muted); line-height: 1.6; }
  .celebs__btn {
    position: absolute; top: 180px; transform: translateY(-50%);
    width: 44px; height: 44px; border-radius: 50%;
    background: var(--c-white); border: 1px solid var(--c-border); color: var(--c-dark);
    display: flex; align-items: center; justify-content: center;
    cursor: pointer; box-shadow: 0 2px 8px rgba(0,0,0,0.08);
    transition: all 0.25s ease; z-index: 2; opacity: 0; pointer-events: none;
  }
  .celebs__btn.is-active { opacity: 1; pointer-events: auto; }
  .celebs__btn:hover { background: var(--c-dark); color: var(--c-white); box-shadow: 0 4px 16px rgba(0,0,0,0.15); }
  .celebs__btn--prev { left: 8px; }
  .celebs__btn--next { right: 8px; }

  /* ══════════════════════
     PULL QUOTE
     ══════════════════════ */
  .pq { padding: 100px 0; background: var(--c-cream); }
  .pq__block { text-align: center; position: relative; padding: 0; margin: 0; }
  .pq__mark { display: block; font-family: var(--font-display); font-size: clamp(5rem,10vw,7rem); line-height: 0.5; color: var(--c-yellow); margin-bottom: var(--space-md); user-select: none; }
  .pq__text { font-family: var(--font-display); font-style: italic; font-size: clamp(1.05rem,2.2vw,1.3rem); line-height: 1.75; color: var(--c-text); max-width: 640px; margin: 0 auto; }
  .pq__cite { margin-top: var(--space-lg); }
  .pq__cite cite { font-family: var(--font-body); font-size: 0.82rem; font-style: normal; font-weight: 600; color: var(--c-text-muted); }

  /* ══════════════════════
     FEATURED COVERAGE
     ══════════════════════ */
  .feat { padding: 100px 0; }
  .feat__hero {
    background: var(--c-white); border-radius: var(--border-radius-lg,16px); overflow: hidden;
    margin-bottom: var(--space-2xl); box-shadow: var(--shadow-md, 0 4px 12px rgba(0,0,0,0.1));
    transition: box-shadow 0.35s ease;
  }
  .feat__hero:hover { box-shadow: var(--shadow-lg, 0 8px 30px rgba(0,0,0,0.12)); }
  .feat__hero-accent { height: 4px; background: linear-gradient(90deg, var(--c-yellow), rgba(255,218,36,0.15) 80%); }
  .feat__hero-inner { display: grid; grid-template-columns: 1fr auto; gap: var(--space-2xl); padding: var(--space-2xl); align-items: start; }
  .feat__cat { display: inline-block; font-size: 0.58rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.12em; color: var(--c-dark); background: rgba(255,218,36,0.2); padding: 3px 12px; border-radius: 100px; margin-bottom: var(--space-md); }
  .feat__hero-title { font-family: var(--font-display); font-size: clamp(1.1rem,2.5vw,1.45rem); line-height: 1.3; margin-bottom: var(--space-sm); }
  .feat__hero-outlet { font-size: 0.82rem; font-weight: 600; color: var(--c-text-muted); margin-bottom: var(--space-md); }
  .feat__hero-excerpt { font-size: 0.88rem; line-height: 1.8; color: var(--c-text-muted); max-width: 580px; margin-bottom: var(--space-xl); }
  .feat__hero-link { display: inline-flex; align-items: center; gap: 6px; font-size: 0.85rem; font-weight: 600; color: var(--c-dark); text-decoration: none; transition: color 0.2s, gap 0.2s; }
  .feat__hero-link:hover { color: var(--c-yellow-hover,#E6C400); gap: 8px; }
  .feat__hero-deco { display: flex; align-items: flex-start; }
  .feat__hero-qm { font-family: var(--font-display); font-size: clamp(5rem,10vw,8rem); line-height: 0.7; color: rgba(255,218,36,0.12); user-select: none; }

  .feat__grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(300px,1fr)); gap: var(--space-lg); }
  .fc { background: var(--c-white); border-radius: var(--border-radius-lg,16px); overflow: hidden; display: flex; flex-direction: column; transition: box-shadow 0.35s cubic-bezier(0.16,1,0.3,1), transform 0.35s cubic-bezier(0.16,1,0.3,1); }
  .fc:hover { box-shadow: var(--shadow-md); transform: translateY(-3px); }
  .fc__bar { height: 3px; background: linear-gradient(90deg, var(--c-yellow), rgba(255,218,36,0.1) 100%); }
  .fc__body { padding: var(--space-xl); display: flex; flex-direction: column; flex: 1; }
  .fc__top { display: flex; align-items: center; justify-content: space-between; margin-bottom: var(--space-md); }
  .fc__top time { font-size: 0.72rem; color: var(--c-text-light); }
  .fc__title { font-family: var(--font-display); font-size: 0.95rem; line-height: 1.4; margin-bottom: var(--space-xs); }
  .fc__outlet { font-size: 0.82rem; font-weight: 600; color: var(--c-text-muted); margin-bottom: var(--space-md); }
  .fc__excerpt { font-size: 0.8rem; line-height: 1.7; color: var(--c-text-muted); flex: 1; }
  .fc__link { display: inline-flex; align-items: center; gap: 5px; font-size: 0.82rem; font-weight: 600; color: var(--c-dark); text-decoration: none; margin-top: var(--space-lg); padding-top: var(--space-md); border-top: 1px solid var(--c-border); transition: color 0.2s, gap 0.2s; }
  .fc__link:hover { color: var(--c-yellow-hover,#E6C400); gap: 7px; }

  /* ══════════════════════
     ARCHIVE
     ══════════════════════ */
  .archive { padding: 100px 0; background: var(--c-warm-gray); }
  .archive__header { display: flex; flex-wrap: wrap; align-items: flex-end; justify-content: space-between; gap: var(--space-lg); margin-bottom: var(--space-xl); }
  .archive__header .sec-title { margin-bottom: 0; }
  .archive__tabs { display: flex; flex-wrap: wrap; gap: 6px; }
  .ftab { display: inline-flex; align-items: center; gap: 6px; padding: 7px 14px; font-family: var(--font-body); font-size: 0.75rem; font-weight: 600; color: var(--c-text-muted); background: var(--c-white); border: 1px solid var(--c-border); border-radius: 100px; cursor: pointer; transition: all 0.2s; white-space: nowrap; }
  .ftab:hover { border-color: var(--c-dark); color: var(--c-dark); }
  .ftab--active { background: var(--c-dark); border-color: var(--c-dark); color: white; }
  .ftab__ct { font-size: 0.6rem; font-weight: 700; opacity: 0.5; }
  .ftab--active .ftab__ct { color: var(--c-yellow); opacity: 1; }
  .archive__live { font-size: 0.82rem; color: var(--c-text-muted); min-height: 1.2em; margin-bottom: var(--space-md); }

  .yg { margin-bottom: var(--space-lg); }
  .yg__sum { display: flex; align-items: center; gap: var(--space-sm); cursor: pointer; padding: var(--space-sm) 0; list-style: none; }
  .yg__sum::-webkit-details-marker { display: none; }
  .yg__sum::marker { display: none; content: ''; }
  .yg__year { font-family: var(--font-display); font-size: 1.2rem; font-weight: 700; color: var(--c-text); }
  .yg__ct { font-size: 0.65rem; font-weight: 700; color: var(--c-text-light); background: var(--c-white); padding: 2px 8px; border-radius: 100px; }
  .yg__line { flex: 1; height: 1px; background: var(--c-border); }
  .yg__chev { flex-shrink: 0; display: flex; align-items: center; color: var(--c-text-light); transition: transform 0.3s cubic-bezier(0.16,1,0.3,1); }
  .yg[open] .yg__chev { transform: rotate(180deg); }
  .cl { display: flex; flex-direction: column; gap: 1px; background: var(--c-border); border-radius: 12px; overflow: hidden; margin-top: var(--space-md); }
  .ci { display: flex; align-items: flex-start; gap: var(--space-lg); padding: var(--space-lg) var(--space-xl); background: var(--c-white); transition: background 0.2s; position: relative; }
  .ci:hover { background: rgba(255,218,36,0.03); }
  .ci__bar { position: absolute; left:0; top:0; bottom:0; width: 3px; background: var(--c-yellow); transform: scaleY(0); transform-origin: center; transition: transform 0.25s cubic-bezier(0.16,1,0.3,1); }
  .ci:hover .ci__bar { transform: scaleY(1); }
  .ci__content { flex: 1; min-width: 0; }
  .ci__meta { display: flex; flex-wrap: wrap; align-items: center; gap: var(--space-xs) var(--space-sm); margin-bottom: var(--space-xs); }
  .ci__outlet { font-size: 0.78rem; font-weight: 700; color: var(--c-text); }
  .ci__dot { width: 3px; height: 3px; border-radius: 50%; background: var(--c-text-light); }
  .ci__meta time { font-size: 0.78rem; color: var(--c-text-muted); }
  .ci__tag { font-size: 0.55rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.1em; color: var(--c-text-muted); background: var(--c-warm-gray); padding: 2px 8px; border-radius: 4px; }
  .ci__title { font-family: var(--font-display); font-size: 0.92rem; line-height: 1.4; margin-bottom: var(--space-xs); }
  .ci__title a { color: var(--c-text); text-decoration: none; }
  .ci__title a:hover { text-decoration: underline; text-decoration-color: var(--c-yellow); text-underline-offset: 3px; text-decoration-thickness: 2px; }
  .ci__excerpt { font-size: 0.8rem; line-height: 1.65; color: var(--c-text-muted); }
  .ci__arrow { min-width: 38px; height: 38px; display: flex; align-items: center; justify-content: center; color: var(--c-text-light); border-radius: 10px; transition: color 0.2s, background 0.2s; margin-top: 2px; flex-shrink: 0; }
  .ci__arrow:hover { color: var(--c-dark); background: rgba(255,218,36,0.1); }

  /* ══════════════════════
     CTA
     ══════════════════════ */
  .cta { background: var(--c-dark-deep, #1E1F25); contain: layout style; }
  .cta__split { max-width: var(--max-width); margin: 0 auto; padding: 100px var(--page-padding); }
  .cta__content { max-width: 560px; }
  .cta__content h2 { font-family: var(--font-display); font-size: clamp(1.5rem,3vw,2.2rem); color: white; margin-bottom: 16px; line-height: 1.2; }
  .cta__content h2 em { font-style: italic; }
  .cta__content p { font-size: 16px; line-height: 1.8; color: rgba(255,255,255,0.5); margin-bottom: 32px; }
  .cta__actions { display: flex; gap: var(--space-md); flex-wrap: wrap; }
  .btn { display: inline-flex; align-items: center; gap: 8px; padding: 14px 28px; font-family: var(--font-body); font-size: 15px; font-weight: 600; border-radius: 100px; text-decoration: none; transition: all 0.35s cubic-bezier(0.16,1,0.3,1); cursor: pointer; border: 2px solid transparent; white-space: nowrap; }
  .btn-primary { background: var(--c-yellow); color: var(--c-dark); border-color: var(--c-yellow); }
  .btn-primary:hover { background: var(--c-yellow-hover,#e6c100); border-color: var(--c-yellow-hover,#e6c100); box-shadow: 0 4px 20px rgba(255,218,36,0.25); }
  .btn-dark { background: var(--c-dark); color: white; border-color: var(--c-dark); }
  .btn-dark:hover { background: #2a2d28; border-color: #2a2d28; }
  .btn-outline-light { background: transparent; color: white; border-color: rgba(255,255,255,0.2); }
  .btn-outline-light:hover { border-color: rgba(255,255,255,0.5); background: rgba(255,255,255,0.04); }
  .btn-lg { padding: 16px 32px; font-size: 16px; }

  /* ══════════════════════
     RESPONSIVE
     ══════════════════════ */
  @media (max-width: 1024px) {
    .today__split { grid-template-columns: 1fr; min-height: auto; }
    .today__video { position: relative; aspect-ratio: 16/9; }
    .today__content { padding: 48px var(--page-padding); }
    .vid-secondary__split { grid-template-columns: 1fr; }
  }
  @media (max-width: 768px) {
    .stats__row { justify-content: space-between; }
    .stats__div:nth-of-type(2) { display: none; }
    .stats__item { padding: var(--space-sm) var(--space-lg); }
    .feat__hero-inner { grid-template-columns: 1fr; }
    .feat__hero-deco { display: none; }
    .feat__grid { grid-template-columns: 1fr; }
    .archive__header { flex-direction: column; align-items: flex-start; }
    .archive__tabs { width: 100%; overflow-x: auto; flex-wrap: nowrap; scrollbar-width: none; -webkit-overflow-scrolling: touch; }
    .archive__tabs::-webkit-scrollbar { display: none; }
    .ci { flex-direction: column; gap: var(--space-sm); }
    .ci__arrow { display: none; }
    .celebs__btn { display: none; }
    .celebs__item { width: 260px; }
    .celebs__photo { width: 260px; height: 310px; }
  }
  @media (max-width: 480px) {
    .stats__div { display: none; }
    .stats__item { flex: 1 1 45%; }
    .stats__num { font-size: 1.7rem; }
    .celebs__item { width: 240px; }
    .celebs__photo { width: 240px; height: 290px; }
  }
  @media (hover: none) and (pointer: coarse) { .celebs__btn { display: none; } }

  /* ══════════════════════
     REDUCED MOTION
     ══════════════════════ */
  @media (prefers-reduced-motion: reduce) {
    [data-reveal] { opacity:1; transform: none; transition: none; }
    .celebs__photo :global(img) { transition: none; }
    .today__play-circle { animation: none; }
    .today__play:hover { transform: translate(-50%,-50%); }
    .vid-secondary__play:hover { transform: translate(-50%,-50%); }
    .fc:hover { transform: none; }
    .ci__bar { transition: none; }
  }

  /* ══════════════════════
     FOCUS
     ══════════════════════ */
  .ftab:focus-visible, .btn:focus-visible, .fc__link:focus-visible,
  .feat__hero-link:focus-visible, .ci__title a:focus-visible,
  .ci__arrow:focus-visible, .today__play:focus-visible,
  .vid-secondary__play:focus-visible, .celebs__btn:focus-visible,
  .yg__sum:focus-visible {
    outline: 3px solid var(--c-yellow); outline-offset: 2px;
  }
</style>
